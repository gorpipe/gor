# Generated by :compat:generate. Cases whose output changed when the project
# root path changed, so no committed baseline could ever match them.
#
# PIPESTEPS, for one, reports the length of a path. Canonicalising ${ROOT} cannot
# help: the leak is a length, not the path. Each line is also a finding about
# the engine, not merely a case to skip.
cmd.king.flag_sym
cmd.pipesteps.bare
