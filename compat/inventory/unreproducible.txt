# Generated by :compat:generate. Cases whose output differed between runs,
# so no committed baseline could ever match them. An empty list is the
# expected state.
#
# The probe varies the project root's path length between runs and repeats
# one of them, which catches a case that reports a path's length as well as
# one whose row order varies. A case known to be unstable is better named in
# exclusions.yml than left to a probe that catches it only sometimes.
#
# Each line here is a finding about the engine, not merely a case to skip.
